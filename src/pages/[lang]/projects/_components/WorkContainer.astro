---
// Components
import WorkItem from "./WorkItem.astro";

// Utils
import { getRecentProjects } from "../../../../lib/utils/api.utils";

const { lang } = Astro.params;
const { data } = await getRecentProjects(lang);
---

<!-- TODO: responsive -->
<section id="work_container" class="pt-8 md:pt-16 pb-16 md:pb-32">
  <div
    class="container mx-auto grid px-4 md:grid-cols-8 lg:grid-cols-12 gap-8 gap-y-12"
  >
    {
      data.map((item: any) => {
        return <WorkItem {...item} />;
      })
    }
  </div>
</section>
